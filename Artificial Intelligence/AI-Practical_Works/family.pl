% This buffer is for notes you don't want to save.
% If you want to create a file, visit that file with C-x C-f,
% then enter the text in that file's own buffer.

father(ram,jrRam).
mother(sita,jrRam).
couple(ram,sita).
grandfather(jrRam,gopal).
father(gopal,ram).
